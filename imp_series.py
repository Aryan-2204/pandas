import numpy as py
import pandas as pd
'''
#astype
series.astype('int16')

#between
series.between(x,y)

#clip
series.clip(x,y) convert values lower than x to x and values lower than y to y

#drop_duplicates
series.drop_duplicates(keep='last')

#duplicated
series.duplicated()

#isnull()
series.isnull()

#dropna
series.dropna()

#fillna
series.fillna()

#isin()
series.isin([])

#apply
series.apply( lamda x : x.split()[0].upper())

#copy

series.copy()



'''